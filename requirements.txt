# DuckDB Knowledge Base MCP Server Requirements

# Core dependencies
duckdb>=1.4.0
mcp>=0.9.0

# Embedding generation (OpenAI - default)
openai>=1.0.0

# Migration script dependencies
pyyaml>=6.0

# Optional: Local embeddings fallback (large download ~500MB with PyTorch)
# sentence-transformers>=2.2.0
# torch>=2.0.0
